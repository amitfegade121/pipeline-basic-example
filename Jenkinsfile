pipeline {

	agent any
	
	stages {
		
		stage('Init') {
			echo 'Initializing....'
		}
		
		stage('Build') {
			echo 'Building the project...'
		}
		
		stage('Deployment') {
			echo 'Deploying the project...'
		}
	
	}
}